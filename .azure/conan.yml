parameters:
  buildArgs: ''

steps:
  - bash: >
      env PATH="/c/Program Files/Conan/conan:${PATH}"
      bash .conan/scripts/export.sh
      -d new -i 1.5.0
    displayName: "Export recipes"
  - bash: >
      env PATH="/c/Program Files/Conan/conan:${PATH}"
      bash .conan/scripts/build.sh
      ${{ parameters.buildArgs }}
      1.5.0
    displayName: "Build"
